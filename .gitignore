# ── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.log
*.egg-info/
.venv/
env/
.ipynb_checkpoints/

# ── Native build artifacts ───────────────────────────────
*.exe
*.o
*.obj
*.dll
cgal_remesh/build/
cgal_remesh/*.exe

# …but DO ship the prebuilt Windows binaries the installer bundles.
!bin/win64/
!bin/win64/*.exe
!bin/win64/*.dll

# PyInstaller work dirs
installer/build/
installer/dist/
installer/*.spec.bak

# ── CGAL headers (downloaded, header-only ~200 MB) ───────
CGAL-*/
*.tar.xz

# ── Data / intermediates (never committed — machine-local) ─
*.npy
*.inr
*.mesh
*.vtk
*.node
*.elem
*.png
save_data/
CRI_Data/

# ── Doxygen output ───────────────────────────────────────
docs/html/
docs/latex/
docs/xml/

# ── Packaging build artifacts (produced in CI) ───────────
/payload/
/dist/
/AppDir/
installer/macos/root/
installer/macos/scripts/
*.AppImage
*.deb
*.rpm
*.pkg
*.pkg.tar.zst
# Linux/macOS native binaries are built fresh in CI (only win64 is committed).
bin/linux64/
bin/macos/

# ── LaTeX build artifacts (reports) ──────────────────────
*.aux
*.toc
*.out
*.synctex.gz
*.bbl
*.blg
*.fls
*.fdb_latexmk

# …but the report sources, figures and compiled PDFs ARE committed
# (overrides the global *.png ignore above).
!report/*.png
!report2/*.png

# Literature reviews live only locally, not in git.
review1_voxel_reconstruction/
review2_mesh_improvement/

# ── OS / editor ──────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
installer/payload/
